name: Validate CAD Manifest

on:
  push:
    paths:
      - 'cad_manifest.yaml'
  pull_request:
    paths:
      - 'cad_manifest.yaml'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install uv
          uv pip install pyyaml jsonschema

      - name: Validate manifest
        run: python scripts/validate.py cad_manifest.yaml schema/cad_manifest.schema.json
